# Harness Retro — intake 2026-09-14

Source: HARNESS FINDINGS document exported from `/tmp/rsexport` (scratch-app, generic stack, omp/18.1.20, host a0f2cc418da1). Ledger-checked via `harness_ledger` (extension tools not mounted in this session — used the documented bun/stub-ExtensionAPI fallback over `~/.omp/agent/extensions/harness-rsi.ts`, same code path).

## Signals (from the document)

- 1 dead reference: three image comments claim model definitions resolve from a phantom user-layer `~/.omp/agent/models.yml` — 2 counted failed probes to that path in telemetry.
- 10/21 tool failures were path-guessing outside the ghost cluster; 6 bash `isError` flags were exit-code false positives; guards recovered. No image defect.
- No config mismatch: user MCP disable set byte-identical to image defaults; MCP pruning not applicable (window < 14 days).
- pi-knowledge@0.10.0 installed with a live KB store — the AGENTS.md Knowledge-base section is not a dead reference (plan contingency branch holds).

## Proposals — dispositions (all recorded to `~/.omp/harness/ledger.json`, source `/tmp/rsexport export 2026-09-14`)

| target | verdict | disposition | action |
| --- | --- | --- | --- |
| build/library/omp-defaults/agent/models.yml | new | applied | Header rewritten: workspace-layer template truth — bootstrap copies it to `.omp/models.yml`; excluded from user-layer seeding (seed-omp-home.sh); workspace copy merged OVER user layer at startup. Verified exclusion at seed-omp-home.sh header + copy loop (`rel == models.yml → continue`); merge semantics per docs/models.md ("automatically merges .omp/models.yml and .omp/config.yml over user-level configurations"). |
| build/scripts/bootstrap.sh | new | applied | OMP_CONFIG template modelRoles comment corrected to the same merge semantics. `bash -n` clean. |
| build/library/omp-defaults/agent/config.yml | new | applied | modelRoles comment (~64-66) corrected identically. |
| build/Dockerfile (rsi-driver-test-pin-2026-09-14) | duplicate | pending (carried) | Prior ledger entry (driver, 2026-09-14) disposition `pending` — not re-implemented; no Dockerfile edit. |

## Repo-local dogfood

- `.omp/config.yml` (this repo's layer) carried the same phantom user-layer claim in its modelRoles comment — corrected to match verified truth. `.omp/models.yml` here already had a clean header.

## Verification

- `grep "NOT consulted|resolves models from the user layer only|Seeded into"` over `build/`, `.omp/config.yml`, `docs/models.md` → no matches.
- Ledger: 4 entries recorded (total 8); re-check of the same document would yield all-duplicate.
- No commits, pushes, or tags made.

---

## Duplicate re-paste run (2026-09-14, post-intake verification)

The SAME findings document (`/tmp/rsexport` export, 2026-09-14) was pasted a second time to verify ledger dedup. Via the bun/stub-ExtensionAPI fallback over the extension's real `/rsi-intake` handler: all 4 proposals verdicted `duplicate` —

| target | verdict | carried disposition |
| --- | --- | --- |
| build/library/omp-defaults/agent/models.yml | duplicate(2026-09-14, applied) | applied — not re-implemented |
| build/scripts/bootstrap.sh | duplicate(2026-09-14, applied) | applied — not re-implemented |
| build/library/omp-defaults/agent/config.yml | duplicate(2026-09-14, applied) | applied — not re-implemented |
| build/Dockerfile (rsi-driver-test-pin-2026-09-14) | duplicate(2026-09-14, pending) | pending — carried |

Zero edits to `build/` (verified: `git diff --stat -- build/` byte-identical before/after this run — the 4-file diff shown is the pre-existing pending phase implementation, unchanged by the re-paste). Ledger recorded 4 duplicate-carried entries (source `re-paste dedup test 2026-09-14`), total 8 → 12. No commits, pushes, or tags.
